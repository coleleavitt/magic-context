interface:
  display_name: "Magic Context"
  short_description: "Repo-specific patterns and workflows for magic-context"
  default_prompt: "Use the magic-context repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true